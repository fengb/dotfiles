#!/bin/bash


remote="${1:?}"

local_branches=`git branch-list`
git branch-list "$remote" | while read branch; do
  if [[ ! "$local_branches" =~ "$branch" ]]; then
    git push -u $remote :$branch
  fi
done
